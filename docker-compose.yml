# ════════════════════════════════════════════════════════════════
#  ND1 캡스톤 — 기본(baseline) compose
#  기준: Windows + Docker Desktop(WSL2 백엔드) 다수 학생 → 소프트웨어 렌더 고정
#  실행:  docker compose up --build      (※ WSL2 우분투 터미널 권장)
#  접속:  noVNC  http://localhost:8080   /  Foxglove  ws://localhost:8765
# ════════════════════════════════════════════════════════════════
services:
  capstone_env:
    build:
      context: .
      dockerfile: Dockerfile
      network: host
    image: nd1capstone/ros2-gazebo-vnc:humble-1.3-tb3
    container_name: nd1_capstone_dev
    ports:
      - "8080:80"
      - "8765:8765"
    volumes:
      - ./workspace:/home/ubuntu/ros2_ws/src
      - ./foxglove:/home/ubuntu/foxglove
      - nd1_build_cache:/home/ubuntu/ros2_ws/build
      - /tmp/.X11-unix:/tmp/.X11-unix   # 호스트 X11 소켓 공유 → DISPLAY=:0 사용 가능
    environment:
      - DISPLAY=:1
      - RESOLUTION=1920x1080
      - VNC_PW=nd1capstone
      - QT_X11_NO_MITSHM=1
      - NVIDIA_VISIBLE_DEVICES=all
      - NVIDIA_DRIVER_CAPABILITIES=all
      - LIBGL_ALWAYS_SOFTWARE=0   # Dockerfile ENV 덮어쓰기 → GPU 렌더링 활성화
      - GALLIUM_DRIVER=
      # ── Node A(LLM) — .env 에서 주입 ──
      - GROQ_API_KEY=${GROQ_API_KEY}
      - GROQ_MODEL=${GROQ_MODEL:-openai/gpt-oss-20b}
      # ── ROS2 ──
      - ROS_DOMAIN_ID=${ROS_DOMAIN_ID:-42}
      - RMW_IMPLEMENTATION=rmw_fastrtps_cpp
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
    restart: unless-stopped   # Docker 데몬 재시작 시 컨테이너 자동 복구
    security_opt:
      - seccomp:unconfined
    shm_size: '2gb'        # Gazebo + Nav2 + VNC 동시 구동 시 SHM 부족 방지
    tty: true
    stdin_open: true

volumes:
  nd1_build_cache:

# ── (참고) 네이티브 Linux + NVIDIA 학생용 가속 override 예시 ────────
#  docker-compose.gpu.yml 로 분리해 두고:
#    docker compose -f docker-compose.yml -f docker-compose.gpu.yml up
#  override 안에서 LIBGL_ALWAYS_SOFTWARE 제거 + deploy.devices(nvidia) 추가.
